// generate a random number array
let arr =[]
function randomNumber () { 
    for(let i = 0; i <= 7; i++){
        let randNum = Math.floor(50 * Math.random())
        arr.push(randNum)
    }
}
randomNumber()
/////////////////////////////////////////////////////
function mergeSort (array){
    let x = 0;
    let sorted = []
    let left = new Array();
    let right = new Array();
    for (let i = 0; i < Math.floor(array.length/2); i++){
        if (i <)
        left.push(array[i])         
        //if (array.length <= 1){

        //}
    }
}
mergeSort(arr);
/////////////////////////////////////////////////////
console.log (arr)
function mergeSort2 (array){
    let containerArray = []
    let left = []
    let right = []
    for (let i = 0; i < array.length; i++){
        if(i < Math.floor(array.length/2)){
            let subArray = Array.from(Math.floor(array.length/2))
            containerArray.push(subArray);

        }else {
            right.push(array[i]);
        }
    }console.log(left)
    console.log(right)
}
mergeSort2(arr);
/////////////////////////////////////////////////////
function mergeSort (array) {
    let arrayContainer = []
    
    if (array.length > 1){
        for (let i = 0; i < array.length; i++){
            Array.from(array.length), array.map = (value) => {
                
            } 
        }    
    }
}